import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    screen: {
        padding: 10,
    },

    registerButton: {
        marginTop: 40,
    },
    loginButton: {
        marginVertical: 20,
    },
});

export default styles;
