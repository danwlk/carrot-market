import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    screen: {
        padding: 10,
    },

    postButton: {
        marginTop: 30,
    },
    backButton: {
        marginTop: 20,
    },
});

export default styles;
